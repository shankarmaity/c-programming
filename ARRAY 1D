#include <stdio.h>

int main()
{
int i;
	char arr[5];
  printf("enter the element of array:");
  for( i=0;i<5;i++)
  {
    scanf("\n%c",&arr[i]);
    fflush(stdin);
   }
  printf("the element of array are:");
   for( i=0;i<5;i++)
  {
    printf("\n%c",arr[i],&arr[i]);
   }
	return 0;
}
